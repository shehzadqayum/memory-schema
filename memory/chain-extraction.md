---
schema: 5
---

Repo born by extraction from helios; first-session setup notes recorded

## Summary

v0.2.0: the unification tag - one write pipeline, scope-aware sync; queue empty until data milestones

## Log

- Step 1: Step recorded from the extraction session (run from the helios venv). This repo was born 2026-07-13 by git subtree split from helios (79 commits) + a seeded memory store (108 entities: 23 helios-era package-dev + 85 curated genesis, see the seed commit + memory-migration-manifest in helios). Remote: github.com/shehzadqayum/memory-schema (main; genesis branch = June history; deployments/helios = consumer state + ledger). FIRST-SESSION SETUP NEEDED HERE: (1) create .env with VOYAGE_API_KEY + NEO4J_PASSWORD; (2) either start this project's own Neo4j (memoryschema neo4j up -> container memory-schema-neo4j) or run JSONL-only (MEMORYSCHEMA_REQUIRE_NEO4J=false) - the store currently has JSONL+embeddings only, Neo4j pending; (3) a venv with pip install -e .[all] (this step ran from the helios venv); (4) memoryschema hook install if Claude sessions will write memories here. Known milestone triggers carried in the migrated memories: multi-space re-test + backend re-benchmark + numeric-probe re-check at the 250-entity corpus milestone; historical git-state replay deliberately deferred.
- Step 2: Package feedback from the fractal pip-path bootstrap (first real pip consumer, install from git+https@v0.1.0 - WORKED: site-packages artifacts deployed via init, per-project hooks registered from site-packages paths, compose parameterization gave fractal-neo4j on 7475/7688 from a TOML-first init). One rough edge found: `plugin sync --check` compares against ALL packaged artifacts while `init --scopes working` (the default) deliberately deploys only the working scope - so every fresh working-scope project reports phantom drift on rules-ondemand/memory-corpus.md until a full `plugin sync`. Fix candidate: record the chosen scopes (e.g. in memoryschema.toml [project] scopes=...) and have sync/--check respect them; or treat not-deployed corpus-scope files as in-scope-absent rather than missing. Also BOOTSTRAP.md was corrected (0db9af6): the old bare `pip install memory-schema[all]` was a dependency-confusion hazard (package is NOT on PyPI); now pinned git+https forms + warning; first tag v0.1.0 cut.
- Step 3: v0.1.1 shipped (e7772d0, tagged). The fractal acceptance test's bug fixed at ALL THREE write paths: index_memory dual-write + auth-fallback and the hook constructed Neo4jMemoryStore() BARE, ignoring memoryschema.toml (wrong bolt endpoint -> auth fail -> silent JSONL-only writes); never surfaced in helios because helios IS the default config. Fixes: config loads BEFORE auth and threads into every construction; both config FALLBACKS use from_toml (the bare MemoryConfig constructor also skips the toml - a second latent copy of the same bug caught during the fix); degradation warnings carry exception detail; hook fall-through loud on stderr; hook quarantine parity (embed_input_hash pop + set_lifecycle - it had drifted behind index_memory; FUTURE WORK: unify the hook onto index_memory to end the duplicate-pipeline drift class); CLI survives cp1252 (--help without PYTHONUTF8); driver notification spam off. l0-echo paraphrase slip = noted as calibration question per doctrine, no change. Regression test test_write_path_config.py pins toml-> construction. PROPAGATED: helios subtree-pulled + reinstalled + plugin 4/4 (first real run of the section-8 update procedure - worked); fractal pip-upgraded @v0.1.1, PROOF write indexed neo4j+jsonl via toml alone (no env override), workaround memory superseded + CLAUDE.md note retired, sync 6/6/6.
- Step 4: Ledger-staleness defect fixed (operator classified it as a bug by the project's own standards - silent + undetectable + produced by the documented workflow). deploy status now: (a) best-effort fetches remote deployments/* refs (a pushed consumer branch no longer reads NOT-PUSHED from an unfetched clone); (b) measures the ledger stamp AND consumer branch against current main - STALE ledger gets the loud warning (re-register is free and must track every consumer update); consumer-branch lag is INFORMATIONAL (consumers legitimately pin between releases - flagging every lag would cry wolf). BOOTSTRAP section 8 gains the re-stamp steps; DEPLOYMENT.md's "can never lie" claim corrected to the honest limit (currency = last re-stamp; status is the loud detector). Live-validated against the real drift (flagged 6-behind ledger + 7-behind branch), then healed: helios re-registered (module@a1b6bb6) + consumer branch re-pushed (0dac3ae). Also: version test un-hardcoded (asserts _version.__version__ - 0.1.1 bump had outrun the string literal). New test test_deploy_staleness.py pins the detector with a git fixture. Suite green.
- Step 5: Refinement after the detector flagged ITSELF (the ship commit moved main past a minutes-old stamp): staleness is now measured against the latest RELEASE TAG, not HEAD - releases are the consumer-consumable units, so module-side chores between tags no longer stale every ledger entry, while the original incident class (a stamp predating a release the consumer actually runs) still fires. Untagged repos fall back to HEAD (the test fixture path). Live: helios reads "ok". Wolf-crying calibrated out in two iterations: HEAD-based -> over-fired on chores; tag-based + informational branch lag = truthful and quiet.
- Step 6: Closeout plan fully executed. fractal published (github.com/shehzadqayum/fractal, operator-created; committed history pushed, working files untouched) and registered in the ledger as the first pip consumer (prefix "pip @ v0.1.1" - the pip semantics land: install=pip, no branch expectation). deploy status: helios ok + fractal ok. Canonical self-hosted: memory-schema-neo4j on 7476/7689, 109/109/109 three-layer, preflight FULL green with the operator-pasted Voyage key. helios: dream pass done (07-04 archived, 07-11 concluded+released, 07-13 active), main fast-forwarded 90 commits and pushed, ledger re-stamped. Clarified for the operator: the memory-schema remote was NEVER created by a session - it pre-existed from the June genesis era (repo-created memory; the preserve-genesis choice proves it); sessions only ever push. Every item from the final audit is now closed; the standing queue is the helios trading roadmap + the 250-entity package milestones.
- Step 7: First organic-usage evidence (fractal, 2026-07-14, one day post-bootstrap): 29 entities (22 active / 7 superseded), 29/29/29 three-layer sync, one active chain. Patterns exercised in the wild exactly as designed: (1) a FOUR-generation supersession chain for an evolving operator preference (render-size 1080p -> 4K -> 800x600 -> 256x144) - the temporal-validity machinery used organically, --as-of replayable; (2) operator CORRECTIONS captured as first-class memories (skew-subordinate-to-composition overriding a mathematical correctness instinct with an aesthetic judgment); (3) domain gotchas of the kind that pay rent (kf-palette-bgr-order: KF colors are BGR-ordered COLORREF - the exact silent-corruption class memories exist for); (4) operator META-directives about memory usage itself (operator-memory-cadence: commit proactively every substantive turn; operator-lessons-per-iteration) - the kernel habits emerging AS operator preferences in a fresh deployment; (5) a living-ledger entity (fractal-aesthetics-lessons) - the evolving-summary pattern outside chains. This is the strongest answer yet to plan-memory-value-measurement's original question ("is the store even read?"): a second project, one day old, with operator-directed active usage, churn, and corrections. Worth watching: whether fractal's recall/citation telemetry accumulates the same way.
- Step 8: v0.1.2 cut and shipped same-day from the 2026-07-14 consumer defect reports (helios dream pass). Fixes: (1) hook cp1252 UnicodeDecodeError - root cause was the hook's hand-rolled store.jsonl scan (inline line 67 = script line 180) reading with the platform codec; hook now exports PYTHONUTF8/PYTHONIOENCODING itself and encoding=utf-8 landed at 19 open() sites across the package incl. the l0_budget WRITE side (cp1252 cannot encode arrows/checkmarks - one em-dash away from the same crash) - the duplicate-pipeline smell strengthens the hook->index_memory unification candidate for v0.2.0; (2) lifecycle status drift - archive/unarchive/reactivate replay onto the JSONL mirror (reactivate also gained .md persistence), diff/local_drift/sync/banner gained status_drift; (3) gate forward-reference advisory on unknown relation targets. 5 regressions (real-hook UTF-8 run with stripped env, script contract, dual-store archive + reactivate, status-drift heal + negative control, gate warning); full suite green. Both consumers upgraded and re-registered same session: helios executed its planned VENDORED->PIP switch at this tag (subtree removed, hooks from site-packages), fractal pip-upgraded (operator tree untouched beyond one CLAUDE.md note). deploy status: fractal ok + helios ok, both pip @ v0.1.2 - the staleness detector correctly flagged fractal as STALE between the tag and its re-register. The report->fix->tag->propagate->re-register loop closed in under a day, second consecutive cycle (v0.1.1: fractal acceptance; v0.1.2: helios dream pass).
- Step 9: v0.2.0 cut same-day as v0.1.2 (the operator asked to implement all actionable future work). The unification: hook-post-write.sh and 'hook test' are thin shims over write_index.index_memory. The inline duplicate had drifted three times across two releases; 'hook test' had degraded further than anyone noticed - it bypassed the write gate entirely (ungated upsert). IndexResult now carries the outcome flags (blocked/skipped/corrupted) as the exit-code contract; corruption triage + MEMORY_GENERATOR moved inside index_memory; hook writes dual-write; declared-v5 corruption is loud (matches reconcile's guard - the old silent skip was documented but inconsistent). Scope-aware plugin sync: presence = opt-in inference + --scopes flag; the fractal bootstrap feedback (phantom corpus drift on fresh projects) is closed. 8 regressions; suite green; both consumers pip @ v0.2.0 in the ledger; obsolete deployments/helios branch deleted (no deployments/* branches remain - pip topology everywhere). THE QUEUE IS NOW EMPTY of code-actionable items: what remains is data-gated (250-entity milestone re-tests: multi-space ablation, backend benchmark, numeric-probe; fractal attribution telemetry read ~2 weeks out).
