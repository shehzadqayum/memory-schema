<memory:entity schema="4" name="chain-release-lesson" type="procedural" importance="8">
  <memory:description>Release chains when the topic concludes — don't accumulate indefinitely into one entity</memory:description>
  <memory:observations>
    <memory:observation>chain-implementing-live-chains accumulated 141 observations across many topic shifts — became incoherent</memory:observation>
    <memory:observation>Each topic shift should have triggered a release and new chain or standalone memory</memory:observation>
    <memory:observation>Editing a prior released memory violates the chain lifecycle: create → update → release → new</memory:observation>
    <memory:observation>A chain with more than ~10-15 observations has likely missed its release point</memory:observation>
  </memory:observations>
  <memory:prompt>Why are you editing a prior memory?</memory:prompt>
  <memory:reasoning>The user caught a behavioral error: I was continuously upsetting one chain entity across multiple distinct topics instead of releasing it and starting new memories. The chain lifecycle rule exists for exactly this reason — a chain should capture ONE coherent reasoning sequence, not become an ever-growing log.</memory:reasoning>
  <memory:chain>learning to use the memory system correctly</memory:chain>
</memory:entity>
