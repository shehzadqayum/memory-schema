<memory:entity schema="4" name="architecture-evaluation" type="knowledge" importance="8" confidence="8">
  <memory:description>Architecture is improving: additive complexity (spaces, combiner, chains) followed by subtractive simplification (trust removal)</memory:description>
  <memory:observations>
    <memory:observation>Phase 1 (sessions 18-21): added 7 spaces, variance combiner, chain entities, framework hardening — additive</memory:observation>
    <memory:observation>Phase 2 (this conversation): removed 3 trust systems, simplified gate 6→4, eliminated Observation subclass — subtractive</memory:observation>
    <memory:observation>Net: more capability (7 spaces, chains, authorisation) with less machinery (0 trust mechanisms, strings not subclasses)</memory:observation>
    <memory:observation>100 entries, 13+9 fields, 7 spaces, 4-stage gate, V1-V12, 627 tests</memory:observation>
    <memory:observation>Cost: 42 tests removed, no per-observation warrant, no external content gating, confidence doesn't score</memory:observation>
    <memory:observation>Remedial report confirmed direction — fixes were removing residual contradictions, not adding mechanisms</memory:observation>
  </memory:observations>
  <memory:prompt>Evaluate if the memory architecture is improving</memory:prompt>
  <memory:reasoning>The trajectory shows a healthy pattern: build capability first, then simplify. The trust removal was the biggest win — three overlapping systems (provenance, basis, source) all solving trust differently were replaced by one field (confidence) that the system preserves but doesn't act on. The architecture is now content-agnostic, consistent (1:1 field-to-space), and self-regulating (variance-weighted combiner).</memory:reasoning>
  <memory:chain>evolving the memory system's data model toward immutability</memory:chain>
</memory:entity>
